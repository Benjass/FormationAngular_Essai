import { SerieTitlePipe } from './serie-title.pipe';

xdescribe('SerieTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new SerieTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
